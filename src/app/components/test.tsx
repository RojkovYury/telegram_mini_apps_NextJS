import { Box } from "@mui/material";

export default function Test() { 
  return(
    <Box sx={{ color: 'var(--tg-theme-button-color)', background: 'var(--tg-theme-text-color)' }}>
      color test
    </Box>
  )
}